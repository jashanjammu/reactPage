import React from 'react';

function App() {
    const styles = {
        textAlign: 'center',
        marginTop: '20%',
    };

    return (
        <div style={styles}>
            <h1>Hello, World!</h1>
        </div>
    );
}

export default App;
